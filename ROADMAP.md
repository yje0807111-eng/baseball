# 개선 로드맵 — 비교 분석(2026-09-25)에서 나온 7단계

비슷한 게임(프로야구 H3 · 컴프매 · FC 온라인 · MLB 더 쇼 · OOTP · 레트로 볼 · TFT)과 견준 분석에서 나온 작업.
**위에서부터 차례로** 진행한다. 새 세션은 이 파일을 먼저 읽고 `진행 중` 또는 첫 `대기` 단계부터 이어 간다.

## 진행 규칙 (단계마다 똑같이)

1. **점검** — 그 단계가 닿는 코드를 전부 찾아 적는다(저장소 · 화면 · 엔진 · 상점 · 보상 · 테스트). "바꾸는 곳"만이 아니라
   "그 값을 읽는 곳"까지. 옛 저장본(이미 플레이한 계정)이 어떻게 되는지도 본다.
2. **결정** — 수치 · 규칙을 정하고 **왜 그 값인지** 이 파일에 적는다. 가능하면 실제 분포 · 시뮬로 확인한 숫자.
3. **작업** — 작은 커밋 여러 개. 한 커밋 = 한 가지.
4. **검증** — 테스트 추가 · `npm run build` · `npx vitest run` · 1920×911 브라우저 확인. 확인용으로 바꾼 저장은 되돌린다.
5. **기록** — 아래 단계의 체크 칸과 `작업 기록`을 채우고 상태를 `완료`로. 다음 단계로 넘어간다.

화면 문구는 CLAUDE.md 의 말투(명사 끝 · 한 마디 · 꾸밈말 금지 · 설명은 규칙 팝업)를 따른다.

---

## 1단계 — 스타터 스쿼드 + 영입에 골드 · `완료` (2026-09-25)

**왜**: 영입 비용이 CP뿐이라 캡 안의 최적 조합이 모두에게 같았다. 새 계정은 빈 라커(`squad: []`)로 시작해
1,566명 가운데 26명을 직접 골라야 첫 경기를 했다. → 시작은 쉽게, 좋은 팀은 벌어서.

### 점검한 곳
- [x] `store.js` — 계정 생성 · 불러오기 · 골드. 영입 · 방출 · 교체 · 스타터 지급을 **골드와 엔트리를 한 번에** 쓰는 함수로
- [x] `rules.js` — `addBlockReason` 에 골드(선택 인자). 교체 영입용 `swapCandidates` · `swapBlockReason`
- [x] `LockerScreen.jsx` — 영입 · 방출 · 자동 채우기 · 목록 행 · 상세 · 거르기
- [x] `SquadBoard.jsx` — 방출 모드는 라커의 `release`(환급)를 탄다. 단추 이름 '빈 자리 채우기'
- [x] `ReadyLocker` · `PrepScreen` — 방출 경로 없음(영향 없음)
- [x] `GameApp.jsx` — 라커 `onSave(team, gold)` 로 상단 골드도 갱신
- [x] `App.jsx` · `LobbyScreen.jsx` — 스타터 지급(선수 데이터는 필요할 때만 받아 옴) · 안내 창
- [x] 드래프트 `협상 대리인` — 드래프트 판 CP 영입가라 무관
- [x] 상점 훈련 아이템 — 선수 객체를 펼쳐 고치므로 `paid` 가 남는다

### 결정 (왜 그 값인가)
- **영입가**: 종합 75 = 150 G, 5 오를 때마다 두 배 (`market.js`). 영입 풀 7,056명 분포 — 가운데 79 · 상위 10% 90 · 상위 1% 100.
  → 가운데 선수 약 260 G, 스타(90) 1,200 G(4승), 100+ 4,800 G(16승). 시작 골드 5,000 이면 첫날 두세 자리 또는 스타 하나.
- **레전드 25% 할증** — 같은 종합이어도 찾는 이름.
- **방출 환급 = 산 값의 절반**(10 G 버림). 스타터 · 무상 채우기 선수는 산 값 0 → 환급 0. 옛 저장본의 선수도 `paid` 가 없어 0
  (공짜로 받은 선수를 팔아 골드를 만드는 길을 막는다).
- **스타터**: 구단 시즌 선수만(외국인 · 레전드 제외 — 영입 목표로 남김), 종합 72~78, SP5 · RP8 · C3 · SS2 · 2B · 3B · 1B · OF5.
  씨앗 = 계정 이름. **AI 상대 승률 36.9%**(160경기 시뮬, `scripts/starter.test.mjs`) — 처음엔 조금 지는 쪽이 많고 영입이 곧 느껴진다.
  캡은 약 400 CP 남는다(초반엔 골드, 후반엔 캡이 막는다).
- **교체 영입**: 스타터로 시작하면 엔트리가 늘 꽉 차 있어 그냥 영입은 막힌다 → 한 명을 내보내며 들인다.
  후보 = 같은 포지션 약한 순 → 같은 유형 약한 순(6명까지 고름). 내보내는 선수 환급까지 골드에 셈한다.
  필수 포지션을 비우는 교체는 막고, 그 이유(`1루수 최소 1명`)를 먼저 보여 준다.
- **빈 자리 채우기**는 무상 — 영입가 100 G 이하(종합 72 안팎) 국내 선수만. 골드가 없어도 엔트리를 맞출 수 있게.
- **코치진은 그대로**(CP만) — 네 자리뿐이고 상점 계약서가 따로 있다. 4단계(상점 정리)에서 다시 본다.

### 한 일
- [x] 영입가 함수 + 방출 환급 규칙 — `market.js`
- [x] 스타터 스쿼드 생성 — `starter.js` · 새 계정 · 빈 라커 계정에 한 번 지급(`team.starterGiven`)
- [x] 영입 골드 차감 · 부족하면 막기(`골드 부족 (N G 모자람)`) · 방출 환급 · 교체 영입
- [x] 빈 자리 채우기를 무상 저가 선수로
- [x] 라커: 목록에 CP · 골드 두 칸, 상세에 남는 캡 · 남는 골드 · 팀 종합, 내보낼 선수 고르기, '영입 가능만' 거르기
- [x] 메인 첫 안내 창(스타터 스쿼드 · 영입 규칙 · 내 라커로)
- [x] 테스트: `market.test.mjs`(영입가 · 환급 · 막는 이유 · 저장 · 교체 15개) · `starter.test.mjs`(규칙 · 씨앗 · 승률 5개)
- [x] 브라우저: 새 계정 → 스타터 26명 · 안내 창 → 교체 영입(5,000 → 200 G) → 방출(+2,400 G) → 빈 자리 채우기(산 값 0)

### 다음 단계로 넘기는 것
- 5단계: 드래프트 런 보상으로 **선수 카드**를 주려면 엔트리 밖에 둘 곳(보관함)이 필요하다. 프리셋 3개도 그 위에서.
- 5단계: 시장 순환(매일 N명) — 지금은 7,056명 전원을 언제든 살 수 있다.

### 작업 기록
- e30323d 영입가 곡선 · 스타터 스쿼드
- ed0e3e2 영입 · 방출 · 스타터 지급을 골드와 한 번에 저장
- c52d115 라커 골드 영입 · 교체 영입 · 환급 · 빈 자리 채우기 · 안내 창
- 5ade675 (덤) 경기 준비 · 대진표 · 랭크 머리의 남은 영어

---

## 2단계 — 증강을 모든 경기에 · `완료` (2026-09-25)

**왜**: 증강은 드래프트 경기에만 있었다. 메인에 '증강' 타일이 내 라커 · 상점과 같은 자리에 있는데, 본 게임(내 팀 경기)과는 이어지지 않았다.

### 점검한 곳
- [x] 드래프트 쪽 구현 — `startGame` 이 `buildTeam(…, augs, teamEnv)` 로 팀을 만들고 `makeAugmentRuntime` 을 중계에 넘김.
  시즌 증강 1장(판 내내) + `MID_AUG_INNINGS=[7]` 1장(그 경기만). 고르기 창 = `ChoiceOverlay`
- [x] `BroadcastGame` — `aug` · `rebuildMy` · `midPickInnings` · `onMidPick`(약속을 기다림) · `askedAt` 가드. 그대로 쓸 수 있음
- [x] `prep.matchTeamOf` — 증강 · 상대 환경을 받도록
- [x] `GameApp.startFromPrep` · `finishMatch` — 일반 대결 · 토너먼트 · 랭크가 모두 여기를 지난다(한 곳만 고치면 세 모드)
- [x] 증강 풀 — 제외(`bannedAugIds`) · 강화 레벨(`withAugLevels`)은 `rollAugmentOptions` 안에서 이미 반영. 지명권은 첫 장에서 챙김
- [x] 상점 증강 권 — 리롤권은 내 팀 경기에서도 쓴다. **즐겨찾기 우대권**은 '한 판' 단위라 드래프트에만 → 4단계에서 정리
- [x] 기록 — 기록 한 줄 `augs`(오른쪽 '쓴 증강' · 메인 랭크 판 증강 TOP 3) · 경기 상세 칩

### 결정
- **정비를 마치며 1장 + 7회 시작에 1장**, 둘 다 그 경기에서만. 드래프트의 두 번(시즌 · 7회)과 같은 박자라 두 모드가 같은 규칙으로 읽힌다.
  메모의 감독 모드("증강은 정비를 마치며 한 장 · 7회에 한 장 두 번뿐")와도 같다.
- 거저 다시 굴리기 한 번(`FREE_REROLL`) → 그다음은 리롤권.
- 창 문구는 '시즌'이 아니라 **경기 증강 · 경기 증강 고르기** (7회는 '7회 증강 고르기').
- AI 상대는 증강 없음(드래프트와 같다).
- 효과형 증강이 보는 상대 값(`envOf`)은 시리즈 팀 로스터에서 바로 센다 — 상대는 `buildTeam` 을 거치지 않으므로.
- 중계 머리에 지금 걸린 증강을 칩으로 — 무엇이 걸렸는지 경기 중에 보이게.
- (덤) 고르기 창 제목 뒤 등급 이름('… 고르기 증강') 삭제 — 등급이 하나로 합쳐져 겹쳐 읽혔다. 중계 머리 'Manager Mode' → '플레이'.

### 한 일
- [x] `matchAug.js` — 7회 · 상대 환경 · 후보 3장(지명권) · 기록용
- [x] `ChoiceOverlay` · `FREE_REROLL` 내보내기, 제목 문구를 받게
- [x] `GameApp` — 정비 끝 고르기 → 증강을 얹은 팀 · 런타임으로 경기 → 7회 고르기 → 기록에 `augs`
- [x] 중계 머리 증강 칩 (`makeAugmentRuntime().list`)
- [x] 테스트 `match-aug.test.mjs` — 후보(제외 · 가진 것 빼기) · 지명권은 첫 장만 · 모든 증강으로 끝까지 경기
- [x] 브라우저: 단판 → 정비 → '경기 증강 고르기' → 칩 1개 → 스킵 → '7회 증강 고르기'(거저 다시 굴리기 한 번 뒤 사라짐) → 칩 2개 → 기록 `augs` 2개

### 작업 기록
- 9f5f840 내 팀 경기에도 증강 — 정비 끝 1장 · 7회 1장 · 중계 머리 칩 · 기록
- bdee821 고르기 창 제목의 겹친 등급 이름

---

## 3단계 — 전술판 잠그기 + 세부 다이얼 삭제 · `완료` (2026-09-25)

**왜**: 경기 중 전술을 공마다 바꿀 수 있어, 한도 3번짜리 승부처 지시 옆에 무제한 조작 창구가 있었다(감독 모드 메모: "자주 묻는데 무게가 가벼운 것이 가장 나쁘다").

### 점검한 곳
- [x] `BroadcastGame` 전술 상태 — `sides` · `touched` · `fineRef`(공마다 `tacticOrders` 가 읽음) · 공수 교대 훅(`sideRef`)
- [x] 정비 전략실(`ReadyLocker.SideBlock`) — 세 갈래 + '더보기'로 펼치는 세부 눈금 7개(`FINE`)
- [x] `strategy.js` — `planOfSides(sides, touched)` · `untouch`
- [x] `tactics.js` · 전술 테스트 — 눈금 값은 갈래가 정하는 데이터로 계속 쓴다
- [x] **발견 ①** 내 팀 경기: `matchTeamOf` 가 `plan` 을 떨어뜨려, 정비에서 무엇을 골라도 경기는 늘 기본 전술(빅볼 · 선발 완주 · 정석)로 시작했다
- [x] **발견 ②** 드래프트: 정비 시작 단추가 계획(plan)을 `startSeason(g)` 의 탑 오르기 인자로 넘겼다 — 증강을 다 고른 뒤 다시 정비에서 시작하면 탑 화면으로 빠질 수 있었다. 드래프트 경기도 계획을 받지 않았다
- [x] `GamePlan.jsx` — 어디서도 쓰지 않는 옛 작전판
- [x] 옛 스페이스 미니게임(`ClutchBatting` · `ClutchPitching` · `ClutchOverlay`) — 개발용 데모 주소(`?demo=crisis|clutch`)로만 열림. `runSimulation`(균형 테스트용 옛 시뮬)의 `onClutch` 훅은 테스트가 쓰지 않지만 엔진 쪽이라 남김

### 결정
- **정비 계획 = 경기의 첫 전술.** 내 팀 · 드래프트 모두.
- **경기 중 변경은 예약 → 다음 공수 교대에 적용, 경기당 3번**(`TACTIC_CHANGES`). 한 교대에 세 갈래를 함께 바꿔도 1번.
  예약을 지금 것으로 되돌리면 취소(횟수 안 씀). 첫 공 전에는 정비의 연장이라 바로 적용 · 횟수 안 씀. 다 쓰면 고르기 판이 잠긴다.
  승부처 지시(3번)와 같은 무게 — "흐름을 보고 몇 번 크게 트는 선택".
- **세부 눈금 삭제**: 갈래 하나가 눈금 여럿을 함께 정한다. 옛 저장본의 `plan.touched` 는 읽지 않는다(보이지 않는 값이 전술을 바꾸지 않게).
- 전략실 머리의 영어 갈래 이름(OFFENSE 등)도 삭제 — 옆에 한글 이름이 있다.

### 한 일
- [x] `BroadcastGame` — 예약(`tacQ`) · 공수 교대 적용(`applyQueued`) · 남은 변경 표시 · 버튼에 '발야구 → 출루 예약'
- [x] `ReadyLocker` — 세 갈래 × 네 안만, 시작 단추는 `planOfSides(sides)`
- [x] `strategy.js` — `touched` · `untouch` 제거
- [x] `prep.matchTeamOf` · 드래프트 `makeMy` — 계획을 싣는다 · 드래프트 시작 단추 인자 끊기
- [x] 옛 미니게임 611줄 · `GamePlan.jsx` 삭제
- [x] 테스트: `plan-carry.test.mjs`(계획이 경기 팀에 · 없으면 기본 · 변경 3번) · `tactics.test.mjs`(눈금 없이 갈래 값)
  · (덤) `game-detail.test.mjs` 의 가끔 깨지던 합계 — 치지 않은 9회 말 'X' 를 숫자로 더하던 것
- [x] 브라우저: 정비에서 발야구 → 경기 첫 전술 발야구 → 출루 예약('발야구 → 출루 예약', 남은 3) → 공수 교대 뒤 출루 · 남은 2

### 작업 기록
- 5f47b2d 전술판 — 계획이 첫 전술로 · 공수 교대 때 3번 · 세부 눈금 · 옛 미니게임 · 옛 작전판 삭제

---

## 4단계 — 상점 · 티켓 정리 · `완료` (2026-09-25)

**왜**: 골드 · CP · RP 에 드래프트 권 5종, 증강 권 5종, 부스트 7종 — 가벼운 게임에서 처음 보는 사람이 상점에서 길을 잃는 양이었다.

### 점검한 곳
- [x] `shop.js` 상품 목록 · 권 목록 · `itemEffect` · 팀 부스트(`applyTeamBoost` — 사면 바로 다음 경기에 붙던 것)
- [x] `ShopScreen` 구매 분기 · 보유 표시 · 품절 조건 · 분류 이름(`catLabel` · `catSub`)
- [x] 드래프트 판 — 모드 설정의 우선 지명권 · 우대권 줄, 판 시작의 권 소모, 라이브 판의 보호 · 대리인 단추, 증강 후보(`rollAugmentOptions` 의 pledge · favor)
- [x] `AugmentScreen` — 지명 단추 · 제거권으로 제외 칸 열기 · 옆줄 권 목록
- [x] `store.js` — `pledgeId` · `augShop` · `aug.removeTickets` · `aug.slots`
- [x] 내 팀 경기 증강(`matchAug.js`)의 지명권 처리
- [x] 정비(`PrepScreen` → 드래프트 `ReadyScreen` → `ReadyLocker`) — 카드 목록이 중간에서 끊기지 않게
- [x] 라커 아이템 탭 — 보관함은 훈련 · 계약서만 남는다(선수 부스트 셋이 빠짐)
- [x] 옛 저장본 — 가진 권 · 담아 둔 선수 부스트 · 이미 건 부스트(`team.boosts`)

### 결정
- **부스트 → 준비 카드 셋**(타선 미팅 · 마운드 미팅 · 불펜 데이): 사면 `team.cards` 에 쌓이고, **정비에서 한 장 골라 그 경기에만**.
  사자마자 붙던 방식은 선택이 아니었다 — 이제 정비의 결정(H3 서포트 카드처럼). 선수 한 명짜리 부스트 셋(에너지 드링크 · 집중력 강화 · 파워 스윙)은 뺐다.
  재활 트레이너는 '운영'으로(바로 쓴다). 드래프트 판 정비에는 카드 칸이 없다(내 팀 경제).
- **드래프트 권 5 → 2**: 스카우트 리포트 · 시리즈 지정권. 우선 지명 · 보호 · 협상 대리인은 같이 뽑기에서만 뜻이 있고 판마다 챙길 것이 늘었다.
  판 엔진(`live.js`)의 해당 기능과 그 시험은 남겨 둔다(화면 · 상점에서만 뺐다).
- **증강 권 5 → 2**: 리롤권 · 강화권(낱장 · 3장 묶음). 지명권 · 우대권 대신 **즐겨찾기한 증강이 늘 두 배 잘 나온다**
  (무게를 준 비복원 추출 — 실측 비율 약 1.9배). **제거권 대신 제외 칸은 처음부터 최대 8칸**.
- **옛 저장본은 산 값만큼 골드로 한 번 환급**(`shopV`), 메인에 환급 내역 창. 이미 건 부스트는 남은 경기 수만큼 그대로 먹는다.
- (덤) 고르기 창 · 라커 설명의 '부스트' 문구 정리.

### 한 일
- [x] `shop.js` — 상품 정리 · `CARD_ITEMS` · `addCard` · `spendCard` · `applyCard` · 죽은 `applyTeamBoost` 삭제
- [x] `ShopScreen` — 카드 구매는 쌓기 · 보유 N장 · '준비 카드 / 경기 전 한 장'
- [x] `ReadyLocker` `CardBlock` · `ReadyScreen`/`PrepScreen` 카드 전달 · `GameApp` 카드 한 장 쓰기 → 로스터에 얹기 → 기록 아이템
- [x] `rollAugmentOptions` 즐겨찾기 무게 · 드래프트 판의 권 줄 · 단추 삭제 · `AugmentScreen` 지명 · 제거권 삭제
- [x] `store.withShopCleanup` 환급 · 메인 `RefundNotice`
- [x] 테스트: `match-items`(카드 7개) · `aug-tickets`(증강 칸 · 즐겨찾기 두 배) · `draft-tickets`(두 장) · `match-aug`(즐겨찾기) · `market`(환급 · 새 계정)
- [x] 브라우저: 환급 창(우선 지명권 900 + 우대권 540 = 1,440 G) → 상점 '준비 카드'에서 타선 미팅 구매(보유 1장)
  → 정비 카드 칸(타선 미팅 1장 · 나머지 0장 잠김) → 경기(카드 1 → 0) → 기록 아이템 '타선 미팅 · 타자 전원' · 증강 화면 제외 칸 0/8 · 지명 단추 없음

### 작업 기록
- e4d2617 상점 정리 — 준비 카드 · 증강 권 · 드래프트 권 · 즐겨찾기 두 배 · 제외 칸 최대 · 환급

---

## 5단계 — 드래프트 런 → 선수 카드 보상 · 조건부 대회 · `완료` (2026-09-25)

**왜**: 모드가 많은데 보상은 모두 골드뿐이고, 드래프트에서 만든 팀은 판이 끝나면 사라졌다. MLB 더 쇼는 모든 모드가 하나의 수집으로 모이고,
OOTP 는 제한을 건 대회로 같은 수집에서 새 판을 만든다.

### 점검한 곳
- [x] 드래프트 판의 끝 — `finishLive`(단판 결과) · `advanceTourney`(토너먼트 대진) · `Gaunt.settle`(탑). 드래프트 모드에는 보상이 전혀 없었다
- [x] 토너먼트 — `makeTournament` · `finishOf` 상금 · 보상 지급(`withTournamentReward`) · 일반 대결 판(`normalPanels` · `TourneyHero`) · 대진표 · 정비 시작 막기(`startBlock`)
- [x] 라커 — 엔트리 밖에 선수를 둘 곳이 없었다(1단계에서 넘긴 일). 상세 판의 세 갈래(내 선수 · 보관함 · 영입 후보)
- [x] 영입가 계산이 한곳(`priceOf`)에 묶여 있어 특가를 얹을 자리(`addBlockReason` 의 값 인자)
- [x] 64강 — 고르는 칸(일반 대결 · 드래프트 설정)과 엔진 · 시험

### 결정
- **64강은 고르는 칸에서만 뺀다** — 6경기라 한 번에 끝내기 길다. 엔진 · 진행 중인 64강 저장본은 그대로 끝까지 한다.
- **보관함(`team.club`) 20명** — 엔트리 밖 보유 선수. CP 에 셈하지 않는다. 엔트리 ↔ 보관함은 공짜, 꽉 찬 엔트리로 들이면 나가는 선수는 보관함으로.
  엔트리 · 보관함을 합쳐 같은 사람은 한 번만(내장 선수는 personId 가 없어 이름으로 구분). 보관함 방출은 산 값의 절반.
- **드래프트 기념 카드** — 한 판에 한 번, 판에서 뽑은 선수 한 명을 보관함으로(산 값 0).
  단판: 판의 첫 승리 · 3장(판에서 가장 좋은 셋 빼고) / 토너먼트 끝: 우승 5장(빼는 사람 없음) · 준우승 4 · 4강 3 · 그 밖 2(가장 좋은 셋 빼고) / 탑 완주 5장.
  가장 좋은 셋을 빼는 것은 판 하나로 최고 선수를 쉽게 데려가지 못하게 — 우승 · 완주만 누구나.
- **오늘의 특가** — 영입을 막지 않고 '들러 볼 이유'만: 날짜(현지 자정) 씨앗으로 매일 12명(선발 2 · 불펜 2 · 포수 · 1 · 2 · 3루 · 유격 · 외야 3),
  종합 82~100, 30% 할인. 모두에게 같은 명단.
- **조건부 대회** — 일반 대결 토너먼트를 열 때 조건 하나: 국내 선수만(×1.3) · 레전드 없이(×1.3) · 2000년 이후 시즌(×1.4) · CP 2,000 이하(×1.5).
  배수는 맞추는 수고. 엔트리에 걸고, 맞지 않으면 일반 대결 판에 이유 · 정비에서 시작을 막는다(보관함 · 영입으로 맞춘다).
- 영입 시장을 '매일 N명만' 으로 막는 순환은 하지 않았다 — 검색으로 7,056명 전원을 사는 지금의 편함을 깨지 않고 특가로 대신했다.

### 한 일
- [x] 64강 제거(고르는 칸)
- [x] 보관함: `store` 넣기 · 들이기 · 방출 · 기념 카드 넣기 / 라커 '보관함' 탭 · 상세 '보관' · '엔트리로'(교체) · 목록은 '보유 · 기념 카드'
- [x] `draft/memento.js` 규칙 · 드래프트 `MementoOverlay` · 세 끝 지점 · 개발용 `?demo=memento`
- [x] `market.dailyDeals` · 라커 '오늘의 특가 12' 칩 · 줄 긋은 원래 값 · 특가 값으로 골드 판정
- [x] `cups.js` · 상금 배수(`finishOf(size, cup)`) · 일반 대결 '대회 조건' 고르기 · 진행 중 조건 표시 · 정비 막기 · 대진표 상금
- [x] 테스트: `memento.test`(5) · `cups.test`(4) · `market.test`(보관함 3 · 특가 4)
- [x] 브라우저: 기념 카드 창 → 데려오기(보관함 · 산 값 0) → 라커 보관함 '교체 · 엔트리로'(나간 선수는 보관함) / 특가 12명 · 할인 값
  / 16강 상금 960 → 국내 선수만 1,250 G · '2000년 이후' 고르면 불충족 경고 → 정비 시작 막힘('조건 불충족 · … 22명')

### 작업 기록
- df80216 64강 삭제 · 739ec75 보관함 · 45bb09d 기념 카드 · 57621aa 보관함 표시 · 데모 · ebe2d29 오늘의 특가 · 85b3064 조건부 대회

---

## 6단계 — 도감 · 주간 과제 · `완료` (2026-09-25)

**왜**: 장기 목표가 등급 · RP · 골드뿐이었다. 1,566명 그림 · 392개 시리즈가 목표가 되지 못했다.

### 점검한 곳
- [x] 선수를 얻는 길 — 영입(`recruitPlayer`) · 교체 영입(`swapPlayer`) · 기념 카드(`addToClub`) · 스타터(`grantStarter`). 보관함 오가기는 새로 얻는 것이 아님
- [x] 시리즈 크기 — 도감 대상(구단 시즌 · 레전드) 392개, 모두 18명
- [x] 과제로 셀 사건의 자리 — 내 팀 경기 끝(`finishMatch`) · 드래프트 경기 끝(`finishLive`) · 토너먼트 열기(`openTourney`) · 토너먼트 끝(`advance`) · 특가 영입(라커) · 기념 카드 받기
- [x] 화면 자리 — 메인 증강 타일은 2단계 뒤 모든 경기와 이어져 그대로 둔다. 도감 · 과제는 기록 화면 메뉴에(기록 = 지난 것 · 모은 것 · 이번 주 할 것)
- [x] 옛 저장본 — `dex` 가 없으면 지금 엔트리 · 보관함으로 시작

### 결정
- **도감(`account.dex`)** = 한 번이라도 가진 선수 id. 방출해도 남는다.
- **시리즈 보상 6명 100 G · 12명 300 G · 18명 800 G**(합 1,200 G). 되팔기로 이득이 나지 않게 — 가장 싼 시리즈 18명을 사서 모두 되팔아도(절반 환급)
  드는 골드가 보상보다 많다(테스트로 확인). 도감은 돈벌이가 아니라 모으는 목표.
- **주간 과제** — 월요일 시작, 셋(첫째는 늘 승리 · 경기 쪽, 나머지는 새 기능 쪽), 다 받으면 보너스 500 G. 모두에게 같은 주 같은 과제.
  과제 여덟: 승리 5번(400) · 경기 8번(300) · 내 지시로 승률 +10%p 경기(400) · 증강 10장(300) · 조건부 대회 열기(500) · 토너먼트 8강 이상(500) ·
  기념 카드(500) · 오늘의 특가 영입(300). 드래프트 경기도 경기 · 승리 · 지시 · 증강으로 센다.
- 기록 화면 사이드 메뉴: 전체 · 단판 · 토너먼트 · 랭크 · **도감(모은 수)** · **주간 과제(받을 보상 수)**. 메인 기록 타일 설명도 '경기 기록 · 도감 · 주간 과제'.

### 한 일
- [x] `dex.js` · `missions.js` 규칙, `store` — 얻을 때 도감에 올리기 · 도감 보상 · 사건 세기 · 과제 · 보너스 받기 · 옛 저장본 도감 시작
- [x] 사건 연결 — `GameApp`(경기 · 승리 · 지시 · 증강 · 조건부 대회 · 8강) · 라커(특가) · 드래프트(경기 · 기념 카드)
- [x] `ProgressPanels.jsx` — 도감(검색 · 진행 막대 · 6/12/18 칩 · 18명 칸 · 보상 받기) · 주간 과제(세 과제 · 진행 · 받기 · 보너스)
- [x] 테스트 `progress.test.mjs`(도감 4 · 과제 5)
- [x] 브라우저: 도감 '모은 선수 26 / 7,056' · 시리즈 18명 칸 / 주간 과제 셋 · 새 과제 날짜 / 단판 한 판 뒤 과제 기록 경기 1 · 증강 2

### 작업 기록
- b9cd3b9 도감 · 주간 과제

---

## 7단계 — 능력치 점검 · `완료` (2026-09-25)

**왜**: 메모에 "능력치는 기억 기반 초안, 웹 검증 필요"로 남아 있었다. KBO 팬이 가장 먼저 보는 것이 "이 선수가 왜 이 값이냐"다.

### 점검한 곳
- [x] 데이터 파이프라인 — `data/kbo/<연도>.txt`(KBO 기록실 1982~2026) → `series-from-records.mjs` → `stat-to-rating.mjs`(그해 리그 평균 대비 구간표).
  이미 기록 기반으로 만들어져 있었다(메모의 '기억 기반'은 초기 46개 시리즈 이야기)
- [x] 카드마다 `source`(그 선수 기록 한 줄) — 연도 파일에서 이름으로 찾는 것보다 정확(동명이인)
- [x] 차이의 성격 — 7,047장 가운데 수비를 뺀 최대 차이 0~8점이 6천 장 남짓. 몇 점 차이는 인지도 보정(수비 평판 · 이름값)으로 의도된 값
- [x] 검증 스크립트(`validate-series.mjs`)의 "같은 사람 · 같은 시즌은 같은 값" — **동명이인이 personId 를 함께 쓰면 서로 다른 사람의 값이 같아지도록 강제**되고 있었다(원인)

### 결정
- **고치는 카드만 좁게**: 기록 출처가 KBO 기록실이고 필요한 칸이 다 있을 때만(기사 · 위키 한 줄은 'K·BB 미확인' · 시즌 중간 기록이라 기록 값이 오히려 틀린다).
  비교에서 빼는 것 — 수비(평판), 도루 0~3개 타자의 주루(도루로는 발을 잴 수 없다), 20경기 안 된 선발의 체력(시즌 중간 합류).
  한 능력치가 **22점 이상** 어긋나면, 20이닝 안 된 투수는 **100 넘는 극단값**이 있을 때만.
- **같은 카드는 함께**: 여러 시리즈에 나오는 카드는 이름 · 해 · 기록 한 줄이 모두 같을 때만 같이 고친다(personId 로 묶으면 동명이인까지 바뀐다 — 한 번 그렇게 했다가 되돌렸다).
- **동명이인 다섯 쌍을 가른다**: 덜 알려진 쪽에 구분자 personId — 이상훈(삼성) 1993 · 1996, 김상진(해태) 1996, 김태균(롯데) 2003, 이승호(SK) 2003. `dupes.json` 에도 넣어 다시 만들 때 유지.

### 한 일
- [x] `scripts/audit-ratings.mjs` — 요약 · `--list` · `--fix`(수비 평판은 그대로)
- [x] 바로잡은 카드 8장: 1995 롯데 김민호 · 2003 롯데 김태균 · 1993/1996 LG 이상훈 · 1996 OB 김상진 · 2003 LG 이승호(동명이인 값) · 1985 MBC 차준섭 · 1987 OB 김경남(몇 이닝 볼넷 0개로 제구 107~109)
- [x] 동명이인 personId 다섯 · `dupes.json` · 빌려 쓰는 그림 목록(`art-alias`) · 리그 평균 다시 만듦 · `validate-series` 통과
- [x] `audit-ratings.test.mjs` — 기록 한 줄 읽기 · "고칠 카드 0장"을 지킨다 · `data/kbo/README.md` 새 시즌 절차에 점검 한 줄

### 남긴 것
- 기사 · 위키 · 시즌 중간 기록으로 만든 카드(2025 · 2026 LG 등, 레전드 일부)는 기록 한 줄이 불완전해 자동으로 견주지 않았다 — 시즌이 끝난 뒤 KBO 기록실 기록으로 다시 만들 때 함께 점검.

### 작업 기록
- cbf46fc 능력치 점검 스크립트 · 카드 8장 · 동명이인 다섯 쌍

---

## 전체 결과 (2026-09-25)
일곱 단계 모두 완료. 새 기능마다 규칙은 모듈(`market` · `starter` · `matchAug` · `cups` · `dex` · `missions` · `memento`)에,
값의 근거는 위 각 단계 '결정'에 있다. 다음에 손볼 때도 이 파일의 진행 규칙(점검 → 결정 → 작업 → 검증 → 기록)을 따른다.

## 뺄 것 (해당 단계에서 함께)
- ~~옛 스페이스 타이밍 미니게임~~ — 3단계에서 삭제
- ~~토너먼트 64강~~ — 5단계에서 삭제
- ~~전술 세부 다이얼~~ — 3단계에서 삭제
- ~~부스트 · 티켓 대부분~~ — 4단계에서 정리
