# 개선 로드맵 — 비교 분석(2026-09-25)에서 나온 7단계

비슷한 게임(프로야구 H3 · 컴프매 · FC 온라인 · MLB 더 쇼 · OOTP · 레트로 볼 · TFT)과 견준 분석에서 나온 작업.
**위에서부터 차례로** 진행한다. 새 세션은 이 파일을 먼저 읽고 `진행 중` 또는 첫 `대기` 단계부터 이어 간다.

## 진행 규칙 (단계마다 똑같이)

1. **점검** — 그 단계가 닿는 코드를 전부 찾아 적는다(저장소 · 화면 · 엔진 · 상점 · 보상 · 테스트). "바꾸는 곳"만이 아니라
   "그 값을 읽는 곳"까지. 옛 저장본(이미 플레이한 계정)이 어떻게 되는지도 본다.
2. **결정** — 수치 · 규칙을 정하고 **왜 그 값인지** 이 파일에 적는다. 가능하면 실제 분포 · 시뮬로 확인한 숫자.
3. **작업** — 작은 커밋 여러 개. 한 커밋 = 한 가지.
4. **검증** — 테스트 추가 · `npm run build` · `npx vitest run` · 1920×911 브라우저 확인. 확인용으로 바꾼 저장은 되돌린다.
5. **기록** — 아래 단계의 체크 칸과 `작업 기록`을 채우고 상태를 `완료`로. 다음 단계로 넘어간다.

화면 문구는 CLAUDE.md 의 말투(명사 끝 · 한 마디 · 꾸밈말 금지 · 설명은 규칙 팝업)를 따른다.

---

## 1단계 — 스타터 스쿼드 + 영입에 골드 · `진행 중`

**왜**: 지금은 영입에 비용이 CP뿐이라 캡 안의 최적 조합이 모두에게 같다. 새 계정은 빈 라커(`squad: []`)로 시작해
1,566명 가운데 26명을 직접 골라야 첫 경기를 한다. → 시작은 쉽게, 좋은 팀은 벌어서.

### 점검할 곳
- [ ] `store.js` — 계정 생성(`emptyAccount` · `signIn`), 불러오기(`loadAccount` · `withTeam`), 골드(`addGold` · `goldOf`)
- [ ] `rules.js` — `addBlockReason` · `squadIssues` (골드 부족 이유를 어디에 넣을지)
- [ ] `LockerScreen.jsx` — 영입(`add`) · 방출(`release`) · 자동 채우기(`autoFill`) · 목록 행(`PlayerRow`) · 상세(`DetailBody`) · 정렬
- [ ] `GameApp.jsx` — 라커에 넘기는 `onSave` (골드가 함께 바뀌어야 함)
- [ ] 영입가를 보여 주는 다른 곳(드래프트 `협상 대리인`은 드래프트 판 CP라 무관한지 확인)
- [ ] 경기 보상 · 대회 보상 · 상점 가격과의 균형

### 결정
- (작업하며 채움)

### 할 일
- [ ] 영입가(골드) 함수 + 방출 환급 규칙
- [ ] 스타터 스쿼드 생성(규칙 통과 · 캡 여유 · 결정적 씨앗) + 새 계정 · 빈 라커 계정에 지급
- [ ] 영입에 골드 차감, 부족하면 막기 · 방출 환급
- [ ] 자동 채우기를 골드 규칙에 맞게
- [ ] 라커 화면: 가격 표시 · 정렬 · 살 수 있는 선수 거르기
- [ ] 첫 입장 안내(스타터 지급)
- [ ] 테스트 · 빌드 · 브라우저 확인

### 작업 기록
- (작업하며 채움)

---

## 2단계 — 증강을 모든 경기에 · `대기`
일반 대결 · 랭크전(내 팀 경기)에도 증강 두 번(정비 끝 · 7회). 지금은 드래프트 경기에만 있다.
점검: `GameApp.startFromPrep` · `BroadcastGame`의 `aug`/`rebuildMy`/`midPickInnings`/`onMidPick` · 드래프트 쪽 구현(`liveTeams.aug`) ·
증강 풀(제외 · 강화 · 즐겨찾기 · 지명) · 증강 권 상점 · 기록 상세의 `augs`.

## 3단계 — 전술판 잠그기 + 세부 다이얼 삭제 · `대기`
경기 중 전술 변경을 이닝 교대 때만(또는 승부처 지시 1회 소모). 세부 다이얼(`FINE` 7개) 화면에서 제거, 3구역 × 4안만.
점검: `BroadcastGame` 전술 상태 · `tactics.js` · `strategy.js` · 정비 작전판(`GamePlan`) · 전술 테스트.

## 4단계 — 상점 · 티켓 정리 · `대기`
부스트 7종 → 경기 전 1장 끼우는 준비 카드. 드래프트 티켓 5 → 2(새로고침 · 시리즈 지정). 증강 티켓 5 → 2(리롤 · 강화).
점검: `shop.js` · `ShopScreen` · 라커 아이템 탭 · 드래프트 권 사용처 · 증강 권 사용처 · 이미 가진 권의 옛 저장본 환산.

## 5단계 — 드래프트 런 → 선수 카드 보상 · 조건부 대회 · `대기`
드래프트 → 3패까지 연승 도전 → 보상(선수 카드 · 골드). 대회에 조건(왕조만 · 태극마크만 · CP 상한). 영입 시장 순환(매일 N명)도 여기서.
점검: 드래프트 흐름 · 탑 오르기(`GauntletScreen`) · 토너먼트 · 보상표(`rewards.js`) · 1단계 영입가.

## 6단계 — 도감 · 주간 과제 · `대기`
시리즈 완성 보상 · 주간 과제. 메인 증강 타일 자리 재검토.

## 7단계 — 능력치 점검 · `대기`
시리즈별 실제 기록과 대조. 데이터 작업이라 다른 단계와 나란히.

## 뺄 것 (해당 단계에서 함께)
- 옛 스페이스 타이밍 미니게임(`ClutchBatting` · `ClutchPitching` · `ClutchOverlay`) — 3단계
- 토너먼트 64강 — 5단계
- 전술 세부 다이얼 — 3단계
- 부스트 · 티켓 대부분 — 4단계
